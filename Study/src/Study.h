/*
 * Study.h
 *
 *  Created on: Feb 20, 2014
 *      Author: liguifan
 */
#include <iostream>

struct Tuple {
      int index;
      double value;
      Tuple(int x,double y): index(x), value(y) {};
//      TreeNode(int x) : val(x), left(NULL), right(NULL) {}
  };


